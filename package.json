{
  "name": "barescript",
  "version": "1.1.0",
  "description": "Lightweight userscript manager for Chrome",
  "license": "Unlicense",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "release": "node scripts/release.js"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.9.0",
    "@types/chrome": "^0.0.270",
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  },
  "dependencies": {
    "preact": "^10.23.0"
  }
}
