// Minimal content script
// All user code injection is handled dynamically by the background service worker
// via chrome.scripting.executeScript

// This script is kept minimal intentionally
console.log('[barescript] content script loaded');
